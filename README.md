# 🌍 Podorognick-back

Бекенд-сервер для додатка Podorognick — платформи для мандрівників, де можна керувати своїми подорожами та історіями. Побудовано на Node.js, Express та MongoDB.

# 🛠 Технологічний стек

Runtime: Node.js (v20+)

Framework: Express.js

Database: MongoDB (через Mongoose)

Security: JWT (JSON Web Tokens) та Bcrypt для хешування паролів

File Management: Multer та Cloudinary (для завантаження фото)

Documentation: Swagger (OpenAPI)

# 📂 Структура проєкту=

src/models — схеми Mongoose та описи даних.

src/controllers — функції обробки запитів та відправки відповідей.

src/services — основна бізнес-логіка та пряма взаємодія з базою даних.

src/routes — визначення API-ендпоінтів та їх прив'язка до контролерів.

src/middlewares — спільна логіка для авторизації, валідації та завантаження файлів.

src/utils — допоміжні функції, такі як HttpError.js або ctrlWrapper.js.

# 🌿 Правила роботи з Git

1. Стратегія гілок
   Ніколи не пуште зміни напряму в гілку main. Використовуйте такі назви для нових гілок:

feature/назва-фічі — для нового функціоналу (наприклад, feature/auth-login).

fix/назва-бага — для виправлення помилок.

refactor/назва-покращення — для рефакторингу або оптимізації коду.

2. Повідомлення комітів (Commits)
   Старайтеся писати назви комітів англійською (це важливо для майбутнього портфоліо), використовуючи стандарт Conventional Commits:

feat: add user registration endpoint

fix: resolve jwt token expiration error

docs: update readme with instructions

# ⚙️ Як запустити проєкт локально

Встановлений Node.js на вашому комп'ютері.

Доступ до бази даних MongoDB (Atlas або локальна).

# Встановлення

# Клонувати репозиторій:

git clone https://github.com/Eugene3671/podorognick-back.git

# Встановити залежності:

npm install

# Налаштування середовища (.env): Створіть файл .env у кореневій папці та додайте наступні змінні:

PORT=5000

MONGODB*URL=ваше*посилання_на_mongodb

JWT*SECRET=ваш*секретний_ключ

CLOUDINARY*NAME=ім'я*хмари

CLOUDINARY_KEY=ключ

CLOUDINARY_SECRET=секрет

Запуск сервера:

Bash

# Режим розробки (з автоперезапуском через nodemon)

npm run dev

# Продакшн режим

npm start
